import { StyleSheet } from "react-native"

const globalStyles = StyleSheet.create({
  h1: {
    fontSize: 40,
    fontWeight: "500"
  },
  h2: {
    fontSize: 35,
    fontWeight: "100",
    alignSelf: "center"
  }
})

export default globalStyles
